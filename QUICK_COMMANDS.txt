# БЫСТРЫЕ КОМАНДЫ ДЛЯ ИСПРАВЛЕНИЯ БАГА КАТЕГОРИЙ ДОХОДОВ

## 1. Обновление бота на сервере
cd /home/batman/expense_bot && bash scripts/quick_bot_update.sh

## 2. Отправка уведомления пользователю 411977529 (ПОСЛЕ ПРОВЕРКИ!)
cd /home/batman/expense_bot && docker-compose exec -T bot python3 notify_user_bug_fixed.py

## 3. Проверка логов пользователя
cd /home/batman/expense_bot && docker-compose logs --tail=100 bot | grep 411977529

## 4. Проверка статуса бота
cd /home/batman/expense_bot && docker-compose ps bot && docker-compose logs --tail=20 bot
