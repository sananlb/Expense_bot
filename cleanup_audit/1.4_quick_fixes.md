# 1.4 Быстрые исправления - Логирование и PII

## 🔴 КРИТИЧНО - Исправить сегодня

### 1. Убрать логирование точных сумм

**Файл:** `bot/services/expense.py:97`
```python
# БЫЛО:
logger.warning(f"User {user_id} tried to add expense with amount too large: {amount}")

# ДОЛЖНО БЫТЬ:
logger.warning(f"User {user_id} tried to add expense exceeding limit (MAX_AMOUNT)")
```

**Файл:** `bot/services/income.py:111`
```python
# БЫЛО:
logger.warning(f"User {user_id} tried to add income with amount too large: {amount}")

# ДОЛЖНО БЫТЬ:
logger.warning(f"User {user_id} tried to add income exceeding limit (MAX_AMOUNT)")
```

**Файл:** `bot/services/income.py:186`
```python
# БЫЛО:
logger.info(f"Created income {income.id} for user {user_id}: {amount} {currency}")

# ДОЛЖНО БЫТЬ:
logger.info(f"Created income {income.id} for user {user_id}")
```

**Файл:** `bot/routers/subscription.py:896-898`
```python
# БЫЛО:
logger.info(f"Payment total_amount: {payment.total_amount}")
logger.info(f"Payment currency: {payment.currency}")
logger.info(f"Payment telegram_payment_charge_id: {payment.telegram_payment_charge_id}")

# ДОЛЖНО БЫТЬ:
logger.info(f"Payment processed for user {user_id}: plan={plan_type}")
# charge_id НЕ логировать - это платёжный идентификатор!
```

---

### 2. Добавить логгер в критичные сервисы

**Файл:** `bot/services/cashback.py` (325 строк, НЕТ логгера)
```python
# Добавить в начало файла:
import logging

logger = logging.getLogger(__name__)

# Добавить логирование в ключевые функции:
# - get_user_cashbacks()
# - create_cashback()
# - update_cashback()
```

**Файл:** `bot/services/subscription.py` (234 строки, НЕТ логгера)
```python
# Добавить в начало файла:
import logging

logger = logging.getLogger(__name__)

# Добавить логирование в:
# - activate_subscription()
# - check_subscription_expired()
# - cancel_subscription()
```

---

## 🟡 ВАЖНО - Исправить на этой неделе

### 3. Добавить user_id в контекст ошибок

**Файл:** `bot/services/ai_categorization.py:183,218`
```python
# БЫЛО:
logger.error("[AICategorizationService] No OpenAI API keys available")

# ДОЛЖНО БЫТЬ:
logger.error(f"[AICategorizationService] No OpenAI API keys available for user {user_id}")
```

**Файл:** `bot/services/currency_conversion.py` (10+ мест)
```python
# Передавать user_id в функции конвертации
# И логировать его при ошибках:
logger.error(f"Currency {from_currency} not found for user {user_id}")
```

---

## 📊 Прогресс исправлений

- [ ] Убрать логирование сумм (4 файла)
- [ ] Добавить логгер в cashback.py
- [ ] Добавить логгер в subscription.py
- [ ] Добавить user_id в ai_categorization.py
- [ ] Добавить user_id в currency_conversion.py
- [ ] Добавить логгер в family.py
- [ ] Добавить логгер в function_call_utils.py
- [ ] Добавить логгер в top5.py

---

## ✅ Проверка после исправлений

```bash
# 1. Проверить что суммы не логируются:
grep -rn "logger.*amount.*{amount}" bot/services/ bot/routers/

# Должно вернуть 0 результатов в критичных местах

# 2. Проверить что все сервисы имеют логгер:
grep -L "logger\|logging" bot/services/*.py

# Должно вернуть только __init__.py и мелкие файлы

# 3. Проверить что ошибки содержат user_id:
grep -rn "logger.error" bot/services/ai_categorization.py | grep -v "user"

# Должно быть минимум результатов
```
