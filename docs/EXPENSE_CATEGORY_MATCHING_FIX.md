# Исправление логики определения категорий расходов

## 🐛 Описание проблемы

### Симптомы:
- Пользователь переименовал категорию с "Коммунальные услуги и подписки" на "Коммуналка и подписки"
- ИЛИ у пользователя осталось старое название "Коммунальные услуги и подписки", а в коде новое "Коммуналка и подписки"
- AI определяет категорию как "Прочие расходы" (fallback) вместо правильной категории
- В логах: `Category '📱 Коммуналка и подписки' not found in user categories, will use AI`

### Пример реальной траты:
```
Текст: "3100 р потратил на аксессуары для телефонов категория семья"
Ожидалось: категория "Коммунальные услуги и подписки" (по ключевому слову "телефон")
Получилось: категория "Прочие расходы" (fallback)
```

---

## 🔍 Анализ текущей логики

### Шаг за шагом (файл: `bot/utils/expense_parser.py`):

**ШАГ 1-3:** Парсинг базовой информации (сумма, описание) - ✅ работает

**ШАГ 4: Поиск по пользовательским категориям (строки 516-564)**
```python
if profile:
    user_categories = await get_user_categories()
    for user_cat in user_categories:
        # Проверяет название и ключевые слова категорий ПОЛЬЗОВАТЕЛЯ
        if user_cat_lower in text_lower or keyword in text_lower:
            category = get_category_display_name(user_cat, lang_code)
```
✅ Использует РЕАЛЬНЫЕ названия из БД

**ШАГ 5: Поиск по стандартным ключевым словам (строки 570-579)** ❌ ПРОБЛЕМА ЗДЕСЬ!
```python
if not category:
    detected_key = detect_expense_category_key(text_lower)  # → "utilities_subscriptions"
    if detected_key:
        category_key = detected_key
        category = get_expense_category_display_for_key(category_key, lang_code)
        # ↑ БЕРЕТ ИЗ КОДА: "📱 Коммуналка и подписки"
        # А У ПОЛЬЗОВАТЕЛЯ: "📱 Коммунальные услуги и подписки"
```

**ШАГ 6: Проверка существования категории (строки 646-654)**
```python
category_exists = any(
    category.lower() in cat.lower() or cat.lower() in category.lower()
    for cat in user_categories
)
# category = "Коммуналка и подписки" (из кода)
# user_categories = ["Коммунальные услуги и подписки", ...] (из БД)
# НЕ СОВПАДАЕТ! → category_exists = False
```

**ШАГ 7: Вызов AI**
```python
if not category_exists:
    should_use_ai = True
    logger.info(f"Category '{category}' not found in user categories, will use AI")
```

**ШАГ 8: AI возвращает fallback**
- AI получает правильный список категорий из БД
- Но не может определить по тексту "аксессуары" → возвращает "Прочие расходы"

---

## 🎯 Корневая причина

**Файл:** `bot/utils/expense_parser.py`, строка 579
**Проблема:** Использование `get_expense_category_display_for_key()`

Эта функция (`expense_category_definitions.py:299-304`) **ВСЕГДА** возвращает жестко прописанное название из кода:

```python
def get_expense_category_display_name(category_key: str, language_code: str = 'ru') -> str:
    data = EXPENSE_CATEGORY_DEFINITIONS.get(category_key)
    return data['name_ru']  # ← "Коммуналка и подписки" (ИЗ КОДА!)
```

Это не учитывает что:
1. Пользователь может переименовать категорию
2. Старые пользователи имеют старые названия категорий
3. Категории в БД и в коде могут отличаться

---

## ✅ Решение: Вариант 1 (РЕКОМЕНДУЕТСЯ)

### Концепция:
Когда нашли `category_key` по ключевым словам - **НЕ берем название из кода**, а **ищем категорию пользователя** которая соответствует этому ключу.

### Алгоритм:
1. Определили `category_key = "utilities_subscriptions"` (по ключевым словам)
2. Используем УЖЕ загруженные категории пользователя (они загружены ранее на строках 520-555)
3. Для каждой категории из загруженного списка:
   - Проверяем `normalize_expense_category_key(category.name_ru)`
   - Если совпадает с найденным ключом → берем ЕЁ название
4. Используем **реальное название из БД**: "Коммунальные услуги и подписки"
5. Категория найдена, AI не вызывается ✅

### Преимущества:
- ✅ Работает с переименованными категориями
- ✅ Работает со старыми названиями у существующих пользователей
- ✅ Сохраняет быстрое определение по ключевым словам (без AI)
- ✅ Уменьшает нагрузку на AI
- ✅ Не ломает существующий функционал
- ✅ НЕТ дополнительных запросов к БД (использует уже загруженные данные)

---

## 📋 План реализации

### Этап 1: Создать вспомогательную функцию

**Файл:** `bot/utils/expense_parser.py`
**Место:** После строки 444 (перед `parse_expense_message`)

**ВАЖНО:** Используем УЖЕ ЗАГРУЖЕННЫЕ категории (они загружены на строках 520-555), чтобы избежать дублирующих запросов к БД.

```python
def find_user_category_by_key(user_categories_objects: list, category_key: str, lang_code: str = 'ru') -> Optional[str]:
    """
    Находит категорию пользователя по category_key из уже загруженного списка.

    Args:
        user_categories_objects: Список объектов ExpenseCategory (УЖЕ загружены из БД)
        category_key: Ключ категории (например, "utilities_subscriptions")
        lang_code: Код языка для отображения

    Returns:
        Название категории пользователя или None
    """
    from bot.utils.category_helpers import get_category_display_name

    for cat in user_categories_objects:
        # Проверяем name_ru
        cat_key_ru = normalize_expense_category_key(cat.name_ru) if cat.name_ru else None
        if cat_key_ru == category_key:
            return get_category_display_name(cat, lang_code)

        # Проверяем name_en
        cat_key_en = normalize_expense_category_key(cat.name_en) if cat.name_en else None
        if cat_key_en == category_key:
            return get_category_display_name(cat, lang_code)

        # Проверяем старое поле name (для обратной совместимости)
        cat_key_name = normalize_expense_category_key(cat.name) if cat.name else None
        if cat_key_name == category_key:
            return get_category_display_name(cat, lang_code)

    return None
```

**Преимущества:**
- ✅ НЕ делает дублирующий запрос к БД
- ✅ Использует уже загруженные данные
- ✅ Синхронная функция (проще и быстрее)
- ✅ Нет накладных расходов на sync_to_async

### Этап 2: Изменить логику в parse_expense_message

**Файл:** `bot/utils/expense_parser.py`
**Строки:** 570-579

**БЫЛО:**
```python
if not category:
    lang_code = profile.language_code if profile and hasattr(profile, 'language_code') else 'ru'
    detected_key = detect_expense_category_key(text_lower)
    if detected_key:
        category_key = detected_key
        category = get_expense_category_display_for_key(category_key, lang_code)  # ❌ ИЗ КОДА
```

**ДОЛЖНО БЫТЬ:**
```python
if not category:
    lang_code = profile.language_code if profile and hasattr(profile, 'language_code') else 'ru'
    detected_key = detect_expense_category_key(text_lower)
    if detected_key:
        category_key = detected_key

        # Если есть профиль - ищем категорию пользователя по ключу (используя уже загруженные данные)
        if profile and user_categories:
            category = find_user_category_by_key(user_categories, category_key, lang_code)  # ✅ ИЗ ЗАГРУЖЕННЫХ ДАННЫХ

        # Fallback: если нет профиля или не нашли у пользователя - берем из definitions
        if not category:
            category = get_expense_category_display_for_key(category_key, lang_code)
```

### Этап 3: Добавить логирование

**Добавить после строки с `find_user_category_by_key`:**

```python
if category:
    logger.info(f"Found user category '{category}' by key '{category_key}'")
else:
    logger.info(f"User category not found for key '{category_key}', using default")
```

### Этап 4: Тестирование

**Тестовые сценарии:**

1. **Пользователь со старым названием:**
   - В БД: "Коммунальные услуги и подписки"
   - Ввод: "интернет 500"
   - Ожидается: категория "Коммунальные услуги и подписки" ✅

2. **Пользователь переименовал категорию:**
   - В БД: "Моя коммуналка"
   - Ввод: "интернет 500"
   - Ожидается: категория "Моя коммуналка" ✅

3. **Пользователь удалил стандартную категорию:**
   - Нет категории utilities_subscriptions
   - Ввод: "интернет 500"
   - Ожидается: AI определит другую подходящую категорию ✅

4. **Новый пользователь (категории по умолчанию):**
   - В БД: "Коммуналка и подписки" (новое)
   - Ввод: "интернет 500"
   - Ожидается: категория "Коммуналка и подписки" ✅

---

## 📊 Ожидаемый результат

### До исправления:
```
Пользователь: "интернет 500"
Парсер: Нашел ключ "utilities_subscriptions"
Парсер: Взял из кода "Коммуналка и подписки"
Парсер: Не нашел у пользователя (у него "Коммунальные услуги и подписки")
Парсер: Вызываем AI
AI: "Прочие расходы" (fallback)
❌ НЕПРАВИЛЬНО
```

### После исправления:
```
Пользователь: "интернет 500"
Парсер: Нашел ключ "utilities_subscriptions"
Парсер: Ищу категорию пользователя с этим ключом
Парсер: Нашел "Коммунальные услуги и подписки" (из БД)
Парсер: Использую это название
✅ ПРАВИЛЬНО (без вызова AI!)
```

---

## ⚠️ Риски и меры предосторожности

### Риск 1: Производительность
**Проблема:** Может замедлить обработку из-за дополнительного поиска
**Решение:**
- ✅ Функция использует УЖЕ загруженные категории (нет дополнительных запросов к БД)
- ✅ Простой цикл по списку объектов в памяти (быстро)
- ✅ Функция вызывается только если найден `category_key` (не на каждый запрос)

### Риск 2: Обратная совместимость
**Проблема:** Может сломать существующее поведение
**Решение:**
- Fallback на старую логику если категория не найдена
- Детальное логирование для отладки
- Постепенное внедрение с мониторингом

### Риск 3: Edge cases
**Проблема:** Пользователь создал 2 категории с одинаковым ключом
**Решение:**
- Функция вернет первую найденную
- Это нормально - у пользователя не должно быть дублей по ключу
- Если есть дубли - это отдельная проблема для исправления

---

## 📝 Чеклист реализации

- [ ] Создать функцию `find_user_category_by_key()`
- [ ] Изменить логику в `parse_expense_message()` (строки 570-579)
- [ ] Добавить логирование
- [ ] Добавить тесты для 4 сценариев
- [ ] Проверить на локальной БД
- [ ] Закоммитить изменения
- [ ] Деплой на сервер
- [ ] Мониторинг логов (3-7 дней)
- [ ] Анализ метрик AI (уменьшение вызовов)
- [ ] Проверка багов в Sentry

---

## 🚀 Деплой и мониторинг

### Команды для деплоя:
```bash
# Обновить код
cd /home/batman/expense_bot && git pull origin master

# Перезапустить бота
docker-compose restart bot

# Проверить логи
docker-compose logs --tail=200 bot | grep -E "Found user category|User category not found"
```

### Метрики для мониторинга:
1. Количество вызовов AI (должно уменьшиться)
2. Процент правильно определенных категорий (должен вырасти)
3. Ошибки в Sentry (не должно быть новых)
4. Время обработки запроса (не должно увеличиться значительно)

---

**Дата создания:** 2025-11-17
**Статус:** Готов к реализации
**Приоритет:** Высокий (влияет на качество категоризации для всех пользователей)
