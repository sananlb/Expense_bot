# Архивный документ

Этот план больше не актуален в исходном виде.

Текущее состояние проекта:

- `bot/services/unified_ai_service.py` уже использует `AsyncOpenAI`.
- OpenRouter работает только через прямое подключение.
- Отдельная логика сетевого обхода и fallback для OpenRouter удалена из runtime-кода.
