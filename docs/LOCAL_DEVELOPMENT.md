# 🚀 Локальный запуск проекта для разработки и тестирования

## Требования:
- Python 3.11.9
- PostgreSQL 17 (локальная БД)
- Redis (localhost:6379)
- Git Bash или аналогичный терминал
- psutil (`pip install psutil` - для автоматической проверки сервисов)

## ⚡ НОВЫЙ СПОСОБ: Автоматический запуск (рекомендуется)

**Теперь достаточно одной команды!**

`run_bot.py` автоматически проверяет и запускает все необходимые сервисы (Redis, Celery Worker, Celery Beat) если они еще не запущены.

```bash
# Активировать виртуальное окружение (Windows Git Bash)
source venv/Scripts/activate

# Запустить бота (автоматически запустит все сервисы)
python run_bot.py
```

### Что происходит автоматически:
1. ✅ Проверяет запущен ли Redis
2. ✅ Проверяет запущены ли Celery Worker и Beat
3. ✅ Если НЕ запущены → автоматически запускает `start_local_dev.py` в отдельном окне (Windows) или фоне (Linux/macOS)
4. ✅ Ждет пока сервисы запустятся (до 15 секунд)
5. ✅ Очищает AI кеш и FSM состояния
6. ✅ Запускает Telegram бота

### При остановке (Ctrl+C):
- ✅ Останавливает бота
- ✅ Останавливает фоновые сервисы (если они были запущены автоматически)

## 📋 Ручной способ (если нужен больший контроль)

Если хочешь запускать сервисы вручную в отдельных терминалах:

**Терминал 1: Фоновые сервисы**
```bash
source venv/Scripts/activate
python start_local_dev.py
```
Этот скрипт запускает:
- Redis (если не запущен)
- Celery Worker (`--pool=solo` для Windows)
- Celery Beat

**Терминал 2: Telegram бот**
```bash
source venv/Scripts/activate
python run_bot.py
```

## 📋 Статус запущенных сервисов

После успешного запуска вы должны увидеть:

✅ **Redis:** PONG
✅ **Celery Worker:** "Connected to redis://localhost:6379/0" + список из ~21 задачи
✅ **Celery Beat:** "celery beat v5.4.0 is starting" + отправка периодических задач
✅ **Telegram Bot:** "Run polling for bot @showmecoin_bot" + "Бот запущен и готов к работе"

## 🎯 Готово к тестированию!

Бот работает в режиме **polling** (без webhook) и готов принимать сообщения через @showmecoin_bot.

**Доступные функции:**
- ✅ Обработка текстовых и голосовых сообщений
- ✅ AI категоризация (OpenRouter / DeepSeek)
- ✅ Парсинг трат и доходов
- ✅ Celery фоновые задачи
- ✅ Redis кеширование

## 🐛 Типичные проблемы при запуске

**Проблема:** Redis не отвечает
**Решение:** Запустите Redis вручную: `redis-server` (или проверьте что сервис запущен)

**Проблема:** `psutil` not found
**Решение:** Установите: `pip install psutil`

**Проблема:** Процессы не останавливаются при Ctrl+C
**Решение:** Закройте окно с `start_local_dev.py` или используйте `taskkill` для остановки Python процессов

## 🛑 Остановка всех процессов

**Windows:**
```bash
# Найти все Python процессы
tasklist | findstr python

# Убить все Python процессы
taskkill /F /IM python.exe
```

**Linux/macOS:**
```bash
# Найти все Python процессы
ps aux | grep python

# Убить процесс по PID
kill <PID>
```
